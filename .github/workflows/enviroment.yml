name: Environment variables
on: push
jobs:
  run:
    runs-on: ubuntu-latest
    env:
      BRANCH: main
    steps:
      - name: Check variable1
        run: echo "The branch is ${{ env.BRANCH }}"
      - name: Check variable2
        run: echo "The Branch is ${BRANCH}"
      - name: CHeckout
        uses: actions/checkout@v5
        with:
          ref: ${{ env.BRANCH }}
